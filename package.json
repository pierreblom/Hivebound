{
  "name": "hivebound",
  "version": "0.2.0",
  "private": true,
  "description": "An original bee-colony engine-builder prototype.",
  "scripts": {
    "build": "node scripts/build-static.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
